<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans    http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context  http://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/tx       http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- @Component/@Configuration 스캔 -->
    <context:component-scan base-package="org.puppit"/>

    <!-- 1) HikariConfig -->
    <bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>
        <property name="jdbcUrl"
                  value="jdbc:mysql://puppit.cp2aos8cos6t.ap-southeast-2.rds.amazonaws.com:3306/puppit?serverTimezone=Asia/Seoul&amp;useUnicode=true&amp;characterEncoding=UTF-8&amp;sslMode=REQUIRED&amp;allowPublicKeyRetrieval=true"/>
        <property name="username" value="user"/>
        <property name="password" value="puppitgood"/>
    </bean>

    <!-- 2) DataSource -->
    <bean id="hikariDataSource" class="com.zaxxer.hikari.HikariDataSource">
        <constructor-arg ref="hikariConfig"/>
    </bean>

    <!-- 3) MyBatis -->
    <bean id="sqlSessionFactoryBean" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="hikariDataSource"/>
        <property name="configLocation" value="classpath:mybatis/config/mybatis-config.xml"/>
        <property name="mapperLocations" value="classpath:mybatis/mapper/*Mapper.xml"/>
    </bean>

    <bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
        <constructor-arg ref="sqlSessionFactoryBean"/>
    </bean>

    <!-- 4) 트랜잭션 -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="hikariDataSource"/>
    </bean>
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- 5) 파일 업로드 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="maxUploadSize" value="104857600"/> <!-- 100MB -->
        <property name="maxUploadSizePerFile" value="10485760"/> <!-- 10MB -->
    </bean>

    <!-- 6) AWS S3 프로퍼티 -->
    <bean id="awsProperties" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
        <property name="properties">
            <props>
                <prop key="aws.region">ap-northeast-2</prop>
                <prop key="aws.s3.bucket">jscode-upload-images</prop>
            </props>
        </property>
    </bean>

    <!-- @Value 바인딩: AWS 프로퍼티 + application-secret.properties -->
    <context:property-placeholder properties-ref="awsProperties"
                                  ignore-unresolvable="true"/>
    <context:property-placeholder location="classpath:application-secret.properties"
                                  system-properties-mode="OVERRIDE"
                                  ignore-unresolvable="true"/>

 </beans>
